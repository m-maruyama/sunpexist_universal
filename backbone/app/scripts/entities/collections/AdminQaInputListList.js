define([
	"app"
],function(App) {
	'use strict';
	App.module('Entities.Collections', function(Collections, App, Backbone, Marionette, $, _){
		Collections.AdminQaInputListList = Backbone.Collection.extend({
		});
	});
});
