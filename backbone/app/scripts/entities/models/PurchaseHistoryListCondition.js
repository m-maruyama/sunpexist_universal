define(["app"],function(App) {
	'use strict';
	App.module('Entities.Models', function(Models,App, Backbone, Marionette, $, _){
		Models.purchaseHistoryListCondition = Backbone.Model.extend({
			defaults: {
			},
			getReq: function(){
			}
		});
	});
});
