<td>{{cster_emply_cd}}<br>
    {{werer_name}}</td>
<td>{{sex_kbn_name}}</td>
<td>{{{order_kbn}}}</td>
<td>{{snd_kbn}}</td>
<td>{{rntl_sect_name}}<br>
    {{job_type_name}}</td>
<td>
  {{#if wearer_change_button}}
    <button type="button" class="btn btn-primary" title="追加貸与" id="wearer_add_lend" value="{{param}}" {{disabled}}>
      {{wearer_change_button}}
      {{#if wearer_change_red}}
      <span style='color:red'>
        {{wearer_change_red}}
      </span>
      {{/if}}
    </button>
  {{/if}}
  {{#if return_reciept_button}}
  <button type="button" class="btn btn-primary" title="返却伝票ダウンロード" id="download">返却伝票ダウンロード</button>
  {{/if}}
</td>
<td>
  {{#if wearer_change_button}}
    <button type="button" class="btn btn-primary" title="不用品返却" id="wearer_add_lend" value="{{param}}" {{disabled}}>
      {{wearer_change_button}}
      {{#if wearer_change_red}}
      <span style='color:red'>
        {{wearer_change_red}}
      </span>
      {{/if}}
    </button>
  {{/if}}
  {{#if return_reciept_button}}
  <button type="button" class="btn btn-primary" title="返却伝票ダウンロード" id="download">返却伝票ダウンロード</button>
  {{/if}}
</td>
